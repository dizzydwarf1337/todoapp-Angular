import { createActionGroup, emptyProps, props } from '@ngrx/store';

export const StatusActions = createActionGroup({
  source: 'Status',
  events: {
    'Status Statuss': emptyProps(),
    
    
  }
});
